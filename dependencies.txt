pip install flask
    # NOTE
     brew install pkg-config # 1st instruction. Just in case you get an error with pip install flask-mysqldb
     brew install mysql # 2nd instruction. Just in case you get an error with pip install flask-mysqldb
pip install flask-mysqldb
pip install passlib

pip install wtforms
pip install functools


mysql.server start
killall mysqld mysqld_safe